{
  "name": "aiblocker-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/background.js src/content.js --bundle --outdir=dist --format=iife --target=chrome111",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
